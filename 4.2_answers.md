# Module 4.2 Answers

1. Run `d2v.docvecs.most_similar` for Six Flags.

   a. List the top 10 companies nearest `SIIX`.

   Answer: The top ten nearest companies for Six Flags Entertainment Corp. (`SIIX`) were `SIIX` itself, Madison Square Garden Sports (`MSGS`), Cedar Fair (`FUN`), Bally's (`BALY`), Liberty Media Braves Group (`LMCA/7`), Golden Entertainment (`GDEN`), Overstock.com (`OSTK`), Playa Hotels & Resorts (`PLYA`), Monarch Casino & Resort (`MCRI`), and Sinclair Broadcast Group (`SBGI`). What I find interesting here is that the list is not narrowly “theme park only.” Instead, it spreads across firms tied to live entertainment, gaming, leisure destinations, and audience-based consumer experiences. That makes sense to me because Doc2Vec is reading the economic language around how these companies make money, not just matching an industry label.

   b. List the companies in the same GICS sub industry as `SIIX`.

   Answer: The companies in the same GICS sub industry as `SIIX` were Six Flags Entertainment Corp. (`SIIX`), Life Time Group Holdings (`LTH`), and Cedar Fair (`FUN`). This is a much narrower set, which is exactly what I would expect from a classification system designed for consistency rather than semantic nuance.

   c. How do those two lists compare with respect to the identification of comparable business activities?

   Answer: The GICS list is cleaner, but it is also much less expressive. It gives me the formal peer group, while the Doc2Vec list gives me the broader “economic neighborhood” around leisure and experiential spending. Cedar Fair showing up in both lists is a good anchor because it confirms the model can recover the obvious peer. But the model also surfaces casinos, sports, resorts, and media-related names that are not identical businesses yet still compete for discretionary entertainment dollars. So if the question is taxonomy, GICS wins. If the question is business-activity proximity, Doc2Vec is more informative.

2. Run `d2v.docvecs.most_similar` for Foot Locker.

   a. List the top 10 companies nearest `FL`.

   Answer: The top ten nearest companies for Foot Locker (`FL`) were `FL` itself, Boot Barn (`BOOT`), Carter's (`CRI`), Urban Outfitters (`URBN`), a.k.a. Brands (`AKA`), Bed Bath & Beyond (`BBBY`), Abercrombie & Fitch (`ANF`), Children's Place (`PLCE`), Oxford Industries (`OXM`), and Academy Sports and Outdoors (`ASO`). This is a pretty convincing result. The list is concentrated in apparel, footwear, and specialty retail, with only modest drift into adjacent discretionary names. In other words, the model is not just finding generic consumer companies. It is finding firms whose descriptions reflect merchandised retail, branded products, store presence, and customer-facing sales activity.

   b. List the companies in the same GICS sub industry as `FL`.

   Answer: The companies in the same GICS sub industry as `FL` were Foot Locker (`FL`), American Eagle Outfitters (`AEO`), Boot Barn (`BOOT`), Urban Outfitters (`URBN`), Buckle (`BKE`), Abercrombie & Fitch (`ANF`), Guess (`GES`), and Children's Place (`PLCE`). This is a very plausible formal peer set for a specialty apparel and footwear retailer.

   c. How do those two lists compare with respect to the identification of comparable business activities?

   Answer: These two lists line up more closely than the Six Flags case, and I think that is because Foot Locker sits in a much more legible business category. Apparel retail tends to use consistent operating language across firms, so both GICS and Doc2Vec converge on similar peers. The semantic list is still a little broader because it admits retailers that are not in the exact same subindustry but still operate with similar sales logic, merchandising language, and customer behavior. So I would say GICS and Doc2Vec are mostly agreeing here, with Doc2Vec adding a bit more flexibility around what counts as a true comparable.

3. Run `d2v.docvecs.most_similar` for Design Therapeutics.

   a. List the top 10 companies nearest `DSGN`.

   Answer: The top ten nearest companies for Design Therapeutics (`DSGN`) were `DSGN` itself, Alector (`ALEC`), BridgeBio Pharma (`BBIO`), Sorrento Therapeutics (`SRNE`), Morphic Holding (`MORF`), Editas Medicine (`EDIT`), Sana Biotechnology (`SANA`), Nuvation Bio (`NUVB`), Inhibrx (`INBX`), and Celldex Therapeutics (`CLDX`). This is one of the strongest outputs in the assignment because the list is not just “biotech in general.” It leans toward companies described with very similar therapeutic-development and precision-medicine language.

   b. List the companies in the same GICS sub industry as `DSGN`.

   Answer: The same GICS sub industry as `DSGN` contains a very large biotechnology and specialty-pharma set, including firms such as Apellis (`APLS`), Ionis (`IONS`), Twist Bioscience (`TWST`), Invitae (`NVTA`), Relay Therapeutics (`RLAY`), Sana Biotechnology (`SANA`), BridgeBio Pharma (`BBIO`), Editas Medicine (`EDIT`), Alector (`ALEC`), Inhibrx (`INBX`), Sorrento Therapeutics (`SRNE`), Rocket Pharmaceuticals (`RCKT`), Replimune (`REPL`), and many others, including `DSGN` itself. The key issue is that the bucket is so broad that it captures a huge range of research, platform, diagnostic, and therapeutic models.

   c. How do those two lists compare with respect to the identification of comparable business activities?

   Answer: This is the clearest case where Doc2Vec beats GICS for me. The GICS list is too coarse to be especially useful if I care about actual operational comparability. It tells me Design Therapeutics belongs to a biotech class, but that class is extremely heterogeneous. The Doc2Vec list is much narrower and feels more behaviorally meaningful because it prioritizes firms whose descriptions emphasize similar scientific and development-stage language. So if I were screening for real comparables rather than category membership, I would trust the semantic list more.

4. Run `d2v.wv.most_similar` for `oil`.

   a. List the top 10 most similar words identified.

   Answer: The top ten most similar words for `oil` were `ammonia`, `feedstock`, `biodiesel`, `distillate`, `petroleum`, `gasoline`, `refining`, `caustic`, `sulfur`, and `biofuels`. This is a good result because the list is not generic. It lands in a very specific petroleum-processing and energy-products vocabulary.

   b. How is `d2v` identifying the word `oil`?

   Answer: It is identifying `oil` through distributional context, meaning the word gets meaning from the kinds of words and documents it tends to appear around. In this corpus, `oil` is being embedded near refining inputs, fuels, chemical intermediates, and industrial energy terms. That is exactly what I would want. The model is not just saying “oil means petroleum.” It is reconstructing the commercial environment in which oil-related firms describe their businesses.

   c. Is this list relevant?

   Answer: Yes, definitely. It is not a synonym list in the dictionary sense, but it is very relevant in the business-description sense. Words like `petroleum`, `gasoline`, `refining`, and `biofuels` all belong to the same operating domain, which is what matters here.

5. Run `d2v.wv.most_similar` for `restaurant`.

   a. List the top 10 most similar words identified.

   Answer: The top ten most similar words for `restaurant` were `dine`, `applebee`, `casual_dining`, `dining`, `tavern`, `maggiano`, `magic`, `ihop`, `convenience_store`, and `chili`. Most of that list is clearly restaurant-adjacent, mixing general dining terms with brand-level language.

   b. How is `d2v` identifying the word `restaurant`?

   Answer: It is identifying `restaurant` by learning the language neighborhood around restaurant and dining businesses in the corpus. That is why it returns both generic terms like `dining` and `casual_dining` and also specific branded tokens like `applebee` and `ihop`. In other words, the model is learning how restaurant firms talk, not just what the word means in isolation.

   c. Is this list relevant?

   Answer: Yes, overall it is relevant. A few terms are noisier than I would want, but the dominant pattern is still strongly tied to food service and dining. For this kind of semantic modeling, that is more than good enough to be useful.

6. Run `d2v.wv.most_similar` for `energy` and `clean`, both individually and as a combined loading.

   a. List the top 10 most similar words identified for `energy`.

   Answer: The top ten most similar words for `energy` were `renewable_energy`, `engineering_procurement`, `grid`, `ameresco`, `electricity`, `solar`, `energy_storage`, `utility`, `power`, and `electric`. This list feels structurally tied to utilities, power systems, and energy infrastructure rather than just hydrocarbons.

   b. List the top 10 most similar words identified for `clean`.

   Answer: The top ten most similar words for `clean` were `greenhouse`, `harsco`, `contaminated`, `wastewater`, `stewardship`, `waste`, `water`, `emission`, `electric_utilities`, and `fossil`. Compared with `energy`, this list leans more toward environmental management and sustainability language.

   c. List the top 10 most similar words identified for `energy + clean`.

   Answer: The top ten most similar words for the combined `energy + clean` vector were `energy`, `renewable_energy`, `engineering_procurement`, `ameresco`, `electricity`, `grid`, `electric`, `power_generation`, `utility`, and `clean`. The combined vector clearly shifts the semantic center toward a clean-energy transition framing rather than pure environmental remediation or generic power production.

   d. How do the lists differ when these words are run as separate vs. combined loadings?

   Answer: When the words are separate, `energy` pulls toward infrastructure and utility language, while `clean` pulls toward emissions, water, waste, and stewardship language. Once they are combined, the result becomes much more coherent around renewable power, grid modernization, and utility-transition concepts. So the combined vector is doing real semantic synthesis here. It is not just averaging two lists; it is producing a more investment-theme-like representation.

7. Run `d2v.docvecs.most_similar` on the semantic vector built through `d2v.wv` for `social_media + search_engine + advertisement` to find companies aligned with the SOCL ETF theme.

   a. List the top 10 companies nearest `social_media + search_engine + advertisement`.

   Answer: The top ten companies nearest the initial SOCL-style vector were Taboola (`TBLA`), Vimeo (`VMEO`), Yelp (`YELP`), Squarespace (`SQSP`), Poshmark (`POSH`), Magnite (`MGNI`), Shutterstock (`SSTK`), ContextLogic/Wish (`WISH`), Semrush (`SEMR`), and Thryv (`THRY`). I think this is a strong first pass because the names cluster around digital attention, discovery, advertising, platform monetization, creator tools, and online commerce. It is not a pure social-media list, but that is actually fine, because SOCL as a theme is broader than just social networks.

   b. Is this relevant given the semantic vector input?

   Answer: Yes. Given the input terms, I would want firms that sit somewhere in the online discovery, audience engagement, digital marketing, or ad-supported platform space, and this list mostly does that. In that sense, the result is semantically faithful even if it is not literally a list of social networks.

8. Run `d2v.wv.most_similar` for `social_media + search_engine + advertisement`.

   a. List the top 10 most similar words identified for `social_media`.

   Answer: The top ten most similar words for `social_media` were `instagram`, `search_engine`, `marketing_campaigns`, `advertisement`, `advertising_campaign`, `facebook`, `public_relations`, `audience`, `twitter`, and `mobile_apps`. This is exactly the kind of neighborhood I would hope for because it captures both platforms and the monetization/engagement layer around them.

   b. List the top 10 most similar words identified for `search_engine`.

   Answer: The top ten most similar words for `search_engine` were `public_relations`, `social_media`, `marketing_campaigns`, `campaign`, `webinars`, `wish`, `brand_awareness`, `advertise`, `engage`, and `advertising_campaign`. This is a little less literal than the `social_media` list, but still useful because it reflects how firms talk about online customer acquisition and digital marketing.

   c. List the top 10 most similar words identified for `advertisement`.

   Answer: The top ten most similar words for `advertisement` were `social_media`, `newspaper`, `newsletter`, `advertiser`, `instagram`, `youtube`, `yelp`, `advertising_campaign`, `advertises`, and `displayed`. I like this because it spans both legacy advertising language and newer platform/distribution language, which feels realistic for actual company disclosures.

   d. How relevant are those words in the identification of comparable companies for `social_media`, `search_engine`, and `advertisement` respectively?

   Answer: I think they are quite relevant overall. `Social_media` is the strongest of the three because the output is very direct and intuitive. `Search_engine` is less clean semantically, but still meaningful because it sits inside a digital-marketing and engagement neighborhood. `Advertisement` is also strong because it connects ad channels, advertiser language, and platform references. So while the three lists are not equally precise, together they provide a solid vocabulary base for a better thematic vector.

9. Print a series of word frequencies for words specific to the types of comparable companies desired for comparison with the SOCL ETF theme.

   a. List the top word frequencies identified for words related to `social`.

   Answer: For words related to `social`, the top frequencies were `social` with `250` occurrences and `social_media` with `194` occurrences. That matters because it shows the theme is not being built from fringe vocabulary. These terms appear often enough to be part of the corpus’s real semantic structure.

   b. List the top word frequencies identified for words related to `search`.

   Answer: For words related to `search`, the top frequencies were `research` with `780`, `search` with `241`, `researcher` with `97`, `search_engine` with `57`, `research_developmentfor` with `29`, `searching` with `21`, and `researching` with `11`. The main thing this tells me is that simple substring matching introduces noise here. The search family is getting contaminated by research-related terms, which is a good reminder that frequency filters need to be interpreted carefully rather than taken at face value.

   c. List the top word frequencies identified for words related to `advert`.

   Answer: For words related to `advert`, the top frequencies were `advertising` with `563`, `advertiser` with `134`, `advertisement` with `51`, `advertising_campaign` with `20`, and `advertise` with `18`. This is a useful pattern because it suggests the advertising theme is both present and reasonably specific in the corpus.

10. Build custom semantic vectors for `social_media`, `search_engine`, and `advertisement` with the most relevant words identified and run `d2v.docvecs.most_similar` to find companies in alignment.

   a. List the top 10 companies nearest your collection of custom semantic vectors.

   Answer: After expanding the SOCL-style vector with terms like `instagram`, `twitter`, `facebook`, `influencer`, `influencers`, `advertising_campaign`, `advertise`, `advertising`, and `advertiser`, the top ten companies were Taboola (`TBLA`), Vimeo (`VMEO`), Yelp (`YELP`), Magnite (`MGNI`), Shutterstock (`SSTK`), Squarespace (`SQSP`), Poshmark (`POSH`), ContextLogic/Wish (`WISH`), Semrush (`SEMR`), and fuboTV (`FUBO`). This version feels slightly sharper to me because it leans more decisively into firms monetizing audience attention, digital content, ad inventory, and online engagement.

   b. Is this list relevant given the semantic vector input?

   Answer: Yes, I think it is relevant. It is still somewhat broad, but broad in the right way. The list clearly sits inside a digital-media, discovery, advertising, and attention-platform ecosystem, which is exactly the space the input vector was trying to capture.

   c. How does this list compare to your original list of companies for the SOCL ETF?

   Answer: The custom list improves thematic precision a bit. The initial list was already good, but the expanded vector seems to pull the ranking more clearly toward digital media and ad-tech names like Magnite and fuboTV. So I would not call it a dramatic transformation, but I do think it is a meaningful refinement.

11. Run `d2v.docvecs.most_similar` on the semantic vector built through `d2v.wv` for `genomic + crispr + bioinformatics + molecular + diagnostics` to find companies aligned with the ARKG ETF.

   a. List the top 10 companies nearest those semantic vectors.

   Answer: The top ten companies nearest the initial ARKG-style vector were Pacific Biosciences (`PACB`), Fulgent Genetics (`FLGT`), Seer (`SEER`), Invitae (`NVTA`), Twist Bioscience (`TWST`), Quanterix (`QTRX`), Heska (`HSKA`), SomaLogic (`SLGC`), Veracyte (`VCYT`), and Myriad Genetics (`MYGN`). This is a very compelling output because it concentrates on sequencing, testing, diagnostics, and life-sciences tool firms. In other words, the vector is recovering a plausible genomics-and-precision-diagnostics cluster.

   b. Is this list relevant given the semantic vector input?

   Answer: Yes, very much so. The firms returned are tightly aligned with the language of genomics, diagnostics, molecular analysis, and advanced life-sciences platforms. I would describe this as one of the most semantically coherent theme results in the whole assignment.

12. Run `d2v.wv.most_similar` for `genomic`, `crispr`, `bioinformatics`, `molecular`, and `diagnostics`.

   a. List the top 10 most similar words identified for `genomic`.

   Answer: The top ten most similar words for `genomic` were `transcriptome`, `genetic`, `decipher`, `prognostic`, `recurrence`, `proteomic`, `characterize`, `translational`, `percepta`, and `validated`. This is a very rich diagnostics and precision-medicine neighborhood.

   b. List the top 10 most similar words identified for `crispr`.

   Answer: The top ten most similar words for `crispr` were `nuclease`, `immunosequencing`, `ligand`, `nucleic_acid`, `moiety`, `linkers`, `casnnnn`, `sequence`, `antibody`, and `vector`. Some of these are more technical and less readable than the others, but they still point in the right gene-editing and molecular-biology direction.

   c. List the top 10 most similar words identified for `bioinformatics`.

   Answer: The top ten most similar words for `bioinformatics` were `proteomics`, `validated`, `profiling`, `modality`, `proteomic`, `computational`, `version`, `adaptive`, `synthesized`, and `translational`. This feels like a computational biology and analytical workflow neighborhood rather than a purely clinical one, which makes sense.

   d. List the top 10 most similar words identified for `molecular`.

   Answer: The top ten most similar words for `molecular` were `pcr`, `massarray`, `ihc`, `immunoassay`, `chromosome`, `peptide`, `sequencing`, `biological`, `polymerase`, and `reagent`. This is probably the most technically interpretable word list in the assignment because it maps directly to laboratory and assay language.

   e. List the top 10 most similar words identified for `diagnostics`.

   Answer: The top ten most similar words for `diagnostics` were `genomics`, `diagnostic`, `scientific`, `genetics`, `diagnostic_test`, `cardiology`, `immunoassay`, `imaging`, `agena`, and `ivd`. This looks like a very sensible testing and medical-device-adjacent semantic neighborhood.

   f. How relevant are those words in the identification of comparable companies for `genomic`, `crispr`, `bioinformatics`, `molecular`, and `diagnostics` respectively?

   Answer: Overall, I think the word sets are quite relevant, though they vary in clarity. `Genomic`, `molecular`, and `diagnostics` are the strongest because their neighborhoods are both technically meaningful and economically interpretable. `Bioinformatics` is useful, but more abstract. `Crispr` is the weakest in readability, though still directionally correct. Taken together, though, these words are more than good enough to support a genomics-style thematic vector.

13. Build custom semantic vectors for `genomic`, `crispr`, `bioinformatics`, `molecular`, and `diagnostics` with the most relevant words identified in your `d2v.most_similar` test and run `d2v.docvecs.most_similar` to find companies in alignment.

   a. List the top 10 companies nearest your collection of custom semantic vectors.

   Answer: After expanding the ARKG-style vector with `genomics`, `genomic`, `genome`, `genetics`, `biotechnology`, `biotech`, `bioscience`, `biotherapeutics`, `biopharmaceutical`, `biopharma`, `gene_editing`, and `crispr`, the top ten companies were Invitae (`NVTA`), SomaLogic (`SLGC`), Myriad Genetics (`MYGN`), Avid Bioservices (`CDMO`), Editas Medicine (`EDIT`), Design Therapeutics (`DSGN`), Sana Biotechnology (`SANA`), Amicus Therapeutics (`FOLD`), Alector (`ALEC`), and Nektar Therapeutics (`NKTR`). Compared with the initial ARKG list, this custom version tilts more toward therapeutics-oriented biotech and gene-based medicine rather than diagnostics tools alone.

   b. Is this list relevant given the semantic vector input?

   Answer: Yes. Once I broaden the vocabulary beyond genomics tools into biotechnology and gene-editing language, it makes sense that the company list broadens toward therapeutic-development names. So the output is consistent with the vector design rather than feeling like noise.

   c. How does this list compare to your original list of companies for the ARKG ETF?

   Answer: The original ARKG list felt more like a sequencing, diagnostics, and life-sciences-tools basket. The custom list feels more like a genomic-innovation biotech basket, with more therapeutic-development exposure. So I would say the first list is narrower and cleaner, while the custom list is broader and more ambitious. Which one is “better” depends on whether I want a tools-and-diagnostics interpretation of ARKG or a wider genomics-and-biotech interpretation.

14. How does it compare with the list of companies you built for the Top 1000 companies?

   Answer: Compared with the Top 1000 exercise, I think the Middle 1000 results still capture the right themes, but with a bit more noise and a little less obviousness. The strongest clusters are still there, especially in retail and genomics/biotech, but the Middle 1000 names are more niche and the disclosure language is less standardized. That tends to make semantic neighborhoods slightly less stable. So my overall read is that the same Doc2Vec approach still works well, but it works a little more cleanly on the Top 1000 because the data-generating language is generally richer and more consistent.
